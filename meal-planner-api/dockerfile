# ----- Fase 1: Construcción (Build) -----
# Usamos una imagen de Maven con Java 17 para compilar nuestro código
FROM maven:3.8-jdk-17 AS build

# Establecemos el directorio de trabajo dentro del contenedor
WORKDIR /app

# Copiamos solo el pom.xml primero para aprovechar el caché de Docker
COPY pom.xml .

# Copiamos el resto del código fuente
COPY src ./src

# Corremos el comando de Maven para crear el .jar
# -DskipTests se salta los tests para un build más rápido
RUN mvn clean package -DskipTests


# ----- Fase 2: Ejecución (Run) -----
# Usamos una imagen ligera de Java 17 para correr la app
FROM eclipse-temurin:17-jre-focal

WORKDIR /app

# Copiamos el .jar que creamos en la fase 'build' a este nuevo contenedor
COPY --from=build /app/target/meal-planner-api-0.0.1-SNAPSHOT.jar .

# El puerto que nuestra app Spring Boot usa (Render lo detectará)
EXPOSE 8080

# El comando para iniciar la aplicación (el mismo que te di antes)
CMD ["java", "-jar", "meal-planner-api-0.0.1-SNAPSHOT.jar"]